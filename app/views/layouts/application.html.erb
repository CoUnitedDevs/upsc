<!DOCTYPE html>
<html>
<head>
  <title>UPSC</title>
  <%= csrf_meta_tags %>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body class="nav-md login">
  <div class="container body">
    <div class="main_container ">
      <h2 class="image">
          <div class="text-center insight-upsc" style="padding: 80px; color: #fff;">
            <h1>INSIGHTS IAS UPSC CURRENT AFFAIRS</h1>
          </div>
      </h2>
      <% if user_signed_in? %>
      <%= render partial: 'layouts/left_menu' %>
      <%= render partial: 'layouts/top_menu' %>
      <% end %>
      <!-- <%# @user_error_messages =  ""%> -->
      <!-- <div class="right_col" role="main"> -->
        <div class="col-md-12">
          <div class ="pull-right">
            <!-- if notice.present? && !current_user.errors.messages.present? -->
            <% if notice.present? %>
            <div class="alert alert-success alert-dismissible fade in" role="alert" id="alert-fade-out">
              <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">×</span>
              </button>
              <strong><%= notice %></strong>
            </div>
            <% end %>
          </div>
        </div>
        <div class="">
          <%= yield %>
        </div>
      <!-- </div> -->
    </div>
  </div>
</body>
</html>
<style>
h1 {
  position: absolute;
  background: rgb(0, 0, 0); /* Fallback color */
  background: rgba(0, 0, 0, 0.5); /* Black background with 0.5 opacity */
  color: #f1f1f1;
  width: 100%;
  /* padding: 20px; */
  font-weight: 700;
}
.image {
  background-image: url(/assets/book3.png);
}
</style>
<!-- <script>
      $(document).ready(function(){
         backButtonPrevention();
      });
</script> -->
